package com.emo.lkplayer.middlelayer.actioners;

public interface Package_Description {
    /* This interface has no use, but only to contain this text*/

    /* This package named actioner, is meant to have interfaces that will have
    concrete implementations, to pull out all methods/code/calls other then getting
    data from ViewModels. Means, ViewModels will have methods only that return data that
    respective views/activities/fragments need. Other all actions/methods will be done by
    concrete classes of these interfaces
     */
}
